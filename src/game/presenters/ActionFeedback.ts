/**
 * Stub for PR1 — action SFX / hit flashes / flare FX still live in GameScene.
 * Future extraction: playActionSfx, flashHit, enemy tint pulses.
 */
export type ActionFeedbackHooks = {
  /** Reserved for extracted feedback orchestration. */
  noop?: true;
};
