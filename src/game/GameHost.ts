/** Shared layout constants for the Phaser Game scene host. */
export const HUD_TOP = 110;
/** Mission log strip height when open (`l`). Collapsed default is 0. */
export const HUD_BOTTOM_LOG = 112;
/** Height of the always-visible collapsed bottom dock strip. */
export const HUD_BOTTOM_DOCK = 18;
/** Tile-to-tile slide — parallel actors; snappy enough for corridor chains. */
export const MOVE_MS = 130;
