{
  "name": "extraction-window",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run tests/sim tests/map tests/ai tests/game tests/data",
    "test:balance": "vitest run tests/balance",
    "playtest": "tsx scripts/playtest.ts",
    "playtest:smoke": "tsx scripts/playtest.ts --smoke",
    "playtest:cohere": "tsx scripts/cohere.ts"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^3.2.7"
  }
}
